rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // One JSON journal per signed-in user. Nobody else can read or overwrite it.
    match /users/{userId}/journal.json {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
